version: '3.8'

services:
  chromadb:
    image: chromadb/chroma
    ports:
      - 8000:8000

  pgvector:
    image: pgvector/pgvector:pg16
    environment:
      environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - 5432:5432
